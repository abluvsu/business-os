{
  "name": "business-os-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "node -e \"const fs = require('fs'); fs.rmSync('dist', { recursive: true, force: true });\""
  },
  "dependencies": {
    "@business-os/shared": "workspace:*",
    "@business-os/workspace": "workspace:*",
    "@fastify/cors": "^9.0.1",
    "drizzle-orm": "^0.30.10",
    "fastify": "^4.26.2",
    "fastify-type-provider-zod": "^2.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "drizzle-kit": "^0.20.17",
    "tsx": "^4.7.1"
  }
}
